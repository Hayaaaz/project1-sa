VERDICT ON AI-GENERATED RUNBOOK
Source: runbook-ai.txt
Model: GitHub Copilot Chat — GPT‑4o
Human Reviewer: Haya

This file documents all hallucinations found in the AI-generated runbook, classifies each one, and records my human judgment (“human call”) on why it is incorrect or unsafe.

------------------------------------------------------------
1. HALLUCINATION: “Stop nginx to clear any stale workers”
   AI Step: sudo systemctl stop nginx
   Classification: Incorrect assumption / unnecessary action
   Human Call: Stopping nginx is not required for a safe restart and introduces downtime. 
               The verified runbook uses `restart` instead of stop/start.

------------------------------------------------------------
2. HALLUCINATION: “Clear the nginx cache directory”
   AI Step: sudo rm -rf /var/cache/nginx/*
   Classification: Dangerous command / destructive action
   Human Call: This is unsafe. Deleting cache is not part of a restart procedure and 
               `rm -rf` is destructive. This must never appear in a verified runbook.

------------------------------------------------------------
3. HALLUCINATION: “Reload firewall rules to ensure port 80 is open”
   AI Step: sudo ufw reload
   Classification: Fabricated requirement / incorrect assumption
   Human Call: The runbook assumes the system uses UFW and that the firewall rules need to be reloaded. 
               This is not part of the nginx restart and may not apply to the environment.

------------------------------------------------------------
4. HALLUCINATION: “Verify nginx is listening on port 80 using netstat”
   AI Step: netstat -tulpn | grep nginx
   Classification: Deprecated tool/environment assumption
   Human Call: `netstat` is not installed by default on modern systems. 
               The verified runbook uses `curl` instead, which is reliable and portable.

------------------------------------------------------------
5. HALLUCINATION: Missing confirm→capture→change→verify structure
   AI Issue: The AI runbook does not follow the required structure.
   Classification: Structural hallucination / missing required format
   Human Call: The assignment requires this structure. The AI output fails to meet it.

------------------------------------------------------------
6. HALLUCINATION: Assumes nginx restart requires clearing cache, firewall reload, and port checks
   AI Issue: Adds unnecessary, invented steps
   Classification: Fabricated operational steps
   Human Call: These steps are not part of a safe nginx restart and were removed in the verified runbook.

------------------------------------------------------------
7. HALLUCINATION: “If all steps succeed, nginx is fully operational”
   AI Issue: No real health check beyond curl localhost
   Classification: Oversimplification / incomplete verification
   Human Call: The verified runbook includes real health checks (nginx -t, curl, service state). 
               The AI version does not.

------------------------------------------------------------
SUMMARY
The AI-generated runbook contained multiple hallucinations, including unsafe commands, incorrect assumptions, deprecated tools, and missing required structure. Human review was necessary to produce a safe, correct, and verifiable runbook.
